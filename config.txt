# 视频源配置（支持多源，逗号分隔）
# USB 摄像头: 0, 1, 2...
# RTSP 摄像头: rtsp://用户名:密码@IP:端口/路径
# 本地视频: D:\video.mp4
source=0

# USB 摄像头分辨率（仅对 USB 摄像头生效）
resolution=1920x1080

# 检测间隔：每隔 N 帧检测一次
# 1 = 每帧都检测（默认，最高精度）
# 2 = 每 2 帧检测一次（GPU 占用降低约 50%）
# 3 = 每 3 帧检测一次（GPU 占用降低约 66%）
# 5 = 每 5 帧检测一次（GPU 占用降低约 80%）
detect_interval=3

# 是否显示检测窗口
show_window=True
